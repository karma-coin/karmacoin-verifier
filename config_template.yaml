# Server configuration yaml file
# These configs override the default config values

verifier:
  name: Verifier 2
  key_password: [xxx]
  # verifier mnemonic
  key_mnemonic: [xxx]
  # address generated from above key - informational only
  public_key_ss58: [xxx]
  bypass_token: [xxx]

twilio:
  account_sid: [xxx]
  auth_token: [xxx]
  service_id: [xxx]
